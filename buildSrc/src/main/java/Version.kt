object PluginVersion {
    const val KOTLIN_VERSION = "1.4.32"
    const val GRADLE_VERSION = "4.2.0"
    const val NAV_SAFE_ARGS_VERSION = "2.3.0"
}

object AndroidVersion {
    const val MIN_SDK_VERSION = 21
    const val COMPILE_SDK_VERSION = 30
    const val TARGET_SDK_VERSION = COMPILE_SDK_VERSION
    const val BUILD_TOOLS_VERSION = "30.0.3"
    const val VERSION_CODE = 1
    const val VERSION_NAME = "1.0.0"

    const val APPLICATION_ID = "com.emertozd.moviesplus"
}

object Version {

    const val CORE_KTX_VERSION = "1.3.2"
    const val FRAGMENT_KTX_VERSION = "1.3.0"

    // Appcompat
    const val APPCOMPAT_VERSION = "1.2.0"

    // Material
    const val MATERIAL_VERSION = "1.4.0-alpha02"

    // ConstraintLayout
    const val CONSTRAINT_LAYOUT_VERSION = "2.0.1"

    // RecyclerView
    const val RECYCLER_VIEW_VERSION = "1.2.0-beta01"

    // Architecture MVVM & LiveData
    const val LIFECYCLE_VERSION = "2.3.0"

    // Navigation Components
    const val NAVIGATION_VERSION = "2.3.3"

    // Retrofit
    const val RETROFIT_VERSION = "2.9.0"

    // Dagger
    const val DAGGER_HILT_VERSION = "2.35"
    const val DAGGER_HILT_JETPACK_KTX = "1.0.0-alpha03"
    const val DAGGER_HILT_ANDRIODX = "1.0.0"

    // Coroutines
    const val COROUTINES_VERSION = "1.4.2"

    // Chucker
    const val CHUCKER_VERSION = "3.4.0"

    //Moshi
    const val MOSHI_VERSION = "1.11.0"

    //Bindables - DataBinding
    const val BINDABLES_VERSION = "1.0.4"

    //Image Loading
    const val COIL_VERSION = "1.1.1"

}

object TestVersion {

    // AndroidX 
    const val androidXTestCoreVersion = "1.3.0"
    const val androidXTestExtKotlinRunnerVersion = "1.1.2"
    const val archTestingVersion = "2.1.0"

    // Junit5
    const val junitVersion = "4.13.2"

    const val coroutinesTestVersion = "1.4.2"

    // MockWebServer
    const val mockWebServerVersion = "4.8.1"

    // MockK
    const val mockKVersion = "1.10.6"

    // Mockito
    const val mockitoVersion = "2.2.0"

    // Mockito Inline
    const val mockitoInlineVersion = "3.8.0"

    // Mockito Inline
    const val turbineVersion = "0.4.0"
}